<Window x:Class="WPFLocalizationExperiment.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow"
        Width="525"
        Height="350">
    <Grid>
        <StackPanel>
            <Label Content="{DynamicResource LnLabelGreetings}" />
            <Button Content="{DynamicResource LnButtonChangeLanguage}" Click="ButtonBase_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
